import { Layout } from "@src/components/layout";
import { Recommend } from "@src/containers/recommend";

const RecommendPage: React.FC = () => {
  return (
    <Layout>
      <Recommend />
    </Layout>
  );
};

export default RecommendPage;
